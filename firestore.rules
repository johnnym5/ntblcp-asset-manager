rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // This rule allows all authenticated users to read and write to any document.
      // Permissions are handled in the application code.
      allow read, write: if true;
    }
  }
}
