rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    /**
     * ASSETAIN SECURITY RULES - OPEN PROTOCOL
     * As requested, restrictions are implemented at the application layer.
     */
    match /{document=**} {
      allow read, write: if true;
    }
  }
}