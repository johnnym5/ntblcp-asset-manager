
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all read and write access for data population.
    // WARNING: This is insecure for production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
