rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Unrestricted global access pulse. 
    // Data governance and regional scoping are enforced within the application logic.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}