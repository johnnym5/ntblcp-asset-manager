rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read/write access for development without authentication.
    // This removes the `if request.auth != null;` check.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
