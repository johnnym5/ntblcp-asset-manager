
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // WARNING: This is insecure and should only be used for initial setup/debugging.
    // This allows all reads and writes to all collections.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
