
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /assets/{assetId} {
      // Allow anyone to read and write to the assets collection.
      // The application code handles authorization.
      allow read, write: if true;
    }
  }
}
