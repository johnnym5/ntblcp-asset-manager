rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: These rules are insecure and grant public access.
    // They are intended only for initial development and debugging.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
