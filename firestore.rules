rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Only allow authenticated users to read or write to the assets collection.
    match /assets/{assetId} {
      allow read, write: if request.auth != null;
    }
  }
}
