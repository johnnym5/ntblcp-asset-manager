rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Grant universal read and write access to the assets collection.
    // This allows the application code to handle all permission logic.
    match /assets/{assetId} {
      allow read, write: if true;
    }
  }
}
