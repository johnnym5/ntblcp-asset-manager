
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow all reads and writes to all documents.
      // The application code is responsible for enforcing security.
      allow read, write: if true;
    }
  }
}
