'use client';

// This page has been removed as user signups are no longer supported.
// Accounts must be created by an administrator through the settings panel.
export default function SignupPage() {
  return null;
}
